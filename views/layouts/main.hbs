<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="/stylesheets/bootstrap.css">
  <link rel="stylesheet" href="/stylesheets/styles.css">
</head>
<body>
  
  <header>
    <!-- navigation -->
    <nav class="navbar navbar-light bg-light">
      <div class="container">
        <a class="navbar-brand" href="/">
          <img src="https://assets-lighthouse.s3.amazonaws.com/uploads/image/file/6227/restaurant-list-logo.png"
            width="30" height="30" class="d-inline-block align-top" alt="餐廳清單">
          {{#if isAuthenticated}}
            {{#if user.name}}
              {{ user.name }}的餐廳清單
            {{else}}
              我的餐廳清單
            {{/if}}
          {{/if}}
        </a>
        
        <div class="row">
          {{#if isAuthenticated}}
          <div class="col-auto">
            <form action="/restaurants/new" method="get">
              <button type="submit" class="btn btn-secondary">新增餐廳</button>
            </form>
          </div>
          <div class="col-auto">
            <a href="/users/logout" class="btn btn-outline-light bnt-logout">Logout</a>
          </div>
          {{else}}
          <div class="col-auto">
            <a href="/users/login" class="btn btn-outline-light bnt-login">Login</a>
          </div>
          {{/if}}
        </div>
      </div>
    </nav>
  
    <div class="banner"></div>
  </header>

  <!-- partial templates will replace the part of body here -->
  <div class="container mt-3 mb-5">
    {{{ body }}}
  </div>
  
  <script src="/javascripts/popper.js"></script>
  <script src="/javascripts/bootstrap.js"></script>
  <script src="https://kit.fontawesome.com/0553e493c7.js" crossorigin="anonymous"></script>
</body>
</html>